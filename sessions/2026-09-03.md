# Session 2026-09-03

## Summary

- Questions: 60
- Correct: 53
- Accuracy: 88.3%
- Best Streak: — (not recorded in the source result)
- Session Type: Data Engineer Rapid Practice
- Rounds: 3 × 20 questions

## Round Results

| Round | Questions | Correct | Accuracy | Notes |
|---:|---:|---:|---:|---|
| 1 | 20 | 17 | 85% | Initial baseline; Grain and mutable default argument weaknesses surfaced |
| 2 | 20 | 18 | 90% | Grain / Anti-Join / `DISTINCT` reinforcement |
| 3 | 20 | 18 | 90% | First 15 targeted questions on Grain, Fanout, Anti-Join, and `DISTINCT` were all correct |

## Topic Coverage

- SQL: Grain, `JOIN` / `LEFT JOIN`, JOIN Fanout, `COUNT(*)` vs `COUNT(column)`, `GROUP BY`,
  `DISTINCT`, `WHERE` vs `HAVING`, `NOT IN` / `NOT EXISTS`
- PostgreSQL: `PRIMARY KEY`, composite keys, `FOREIGN KEY`, `UNIQUE`, `CHECK`, `ON CONFLICT`
- Data Engineering: ETL / Canonicalization, Data Quality, Business Key / Surrogate Key,
  Idempotency, Snapshot Diff
- Python: Mutable Default Argument, keyword-only arguments, `dict.get()` and key existence
- REST API: Pagination

> The source result provides aggregate round scores and conceptual mistake summaries, but not a
> question-by-question topic mapping. This session therefore does not invent per-topic question
> counts or accuracy.

## Question Breakdown

The source result identifies the following seven conceptual misses; original question numbers,
selected options, correct options, and per-question difficulty were not included.

| Scope | Domain | Topic | Type | Result | Difficulty | Note |
|---|---|---|---|---|---|---|
| R1 | SQL | JOIN output Grain | Concept judgment | Missed | — | A one-to-many join can move the result to order grain. |
| R1 | SQL | `GROUP BY` and Grain | Concept judgment | Missed | — | `GROUP BY` commonly changes the output grain. |
| R1 | Python | Mutable Default Argument | Code reading | Missed | — | A default list is shared across calls when omitted. |
| R2 | SQL | LEFT JOIN Anti-Join | Concept judgment | Missed | — | `WHERE right_col IS NULL` finds unmatched left rows. |
| R2 | SQL | `DISTINCT` and Grain | Concept judgment | Missed | — | Grain depends on the full selected column combination. |
| R3 | Data Engineering | Snapshot Diff | Concept judgment | Missed | — | A-only means `Disappeared`. |
| R3 | Python | `dict.get()` and Key Existence | Code reading | Missed | — | Use membership testing when the value may be `None`. |

## Mistakes Worth Keeping

1. **JOIN output Grain** — A one-to-many join can move the result from customer grain to order
   grain; identify what one row represents after the join.
2. **`GROUP BY` and Grain** — `WHERE` filters rows, while `GROUP BY` aggregates and commonly
   changes the output grain to the grouping keys.
3. **Python Mutable Default Argument** — `items=[]` is created once at function definition;
   use `None` and initialize inside the function.
4. **`LEFT JOIN ... WHERE right_col IS NULL`** — this is a common Anti-Join pattern for finding
   left-side rows with no matching right-side row.
5. **`DISTINCT` and Grain** — determine the grain from the complete selected column combination,
   not from the presence of `DISTINCT` alone.
6. **Snapshot Diff: `Disappeared`** — a key present in Snapshot A but absent in Snapshot B is
   `Disappeared`.
7. **`dict.get()` vs key existence** — `record.get("volume") is None` cannot distinguish a
   missing key from `{"volume": None}`; use `"volume" in record`.

The source result did not include the selected and correct option letters, so those fields are
left unrecorded in the mistake log.

## New Learning

- Grain must be re-evaluated after `JOIN`, `GROUP BY`, and `DISTINCT`.
- Aggregating a measure at the wrong grain after a one-to-many join can create fanout and
  over-counting.
- `COUNT(*)` counts rows, while `COUNT(column)` excludes rows where that column is `NULL`.
- `NOT IN` can produce unexpected results when the subquery contains `NULL`; `NOT EXISTS` is a
  safer anti-join pattern in that situation.
- In a `LEFT JOIN`, conditions in `ON` preserve unmatched left rows; conditions in `WHERE` can
  remove them.
- PostgreSQL `CHECK` expressions that evaluate to `UNKNOWN` do not reject a row by themselves;
  combine `NOT NULL` with `CHECK` when the value is required.
- PostgreSQL allows multiple `NULL` values in a default `UNIQUE` column; add `NOT NULL` when the
  field is required.
- `ON CONFLICT` supports idempotent loading when combined with an appropriate business key or
  unique constraint.
- Canonicalization standardizes source field names and data types before downstream use.
- REST pagination uses `top` for page size and `skip` for the number of records to bypass.

## Mastery Changes

| Topic | Before | After | Reason |
|---|---:|---:|---|
| SQL / NULL and Three-valued Logic | 2.5 | 3.0 | `NOT IN` / `NOT EXISTS` and `NULL` behavior were handled reliably in the session. |
| SQL / JOIN Fanout | 2.5 | 3.0 | Fanout and measure-grain mismatch were explained and later reinforced. |
| SQL / Grain | 2.5 | 2.75 | Significant improvement after targeted practice, but it remains the first review priority. |
| SQL / Subquery and `EXISTS` | 2.5 | 3.0 | `EXISTS` and `NOT EXISTS` were treated as existence tests without JOIN fanout. |
| Python / Functions and Parameters | 2.0 | 2.5 | Keyword-only arguments were stable; mutable defaults still need reinforcement. |
| Database / Constraints and Keys | 2.0–2.5 | 3.0 | Primary, foreign, composite, unique, check, and conflict-handling concepts were reinforced. |
| Data Engineering / ETL and Data Quality | 2.0–2.5 | 3.0 | Canonicalization, validation, quality rules, and idempotency were handled reliably. |
| Data Engineering / Snapshot Diff | Not assessed | 2.0 | Basic classifications were introduced, but `Disappeared` was missed once. |
| API / Pagination | 2.0 | 2.5 | `top` and `skip` pagination semantics were recognized. |

These ratings are provisional because the source result did not provide per-topic counts or
question difficulty.

## Next Session Priorities

1. SQL Grain across mixed `JOIN` / `GROUP BY` / `DISTINCT` cases.
2. Snapshot Diff: `Inserted`, `Changed`, and `Disappeared` direction.
3. Python mutable default arguments and `dict.get()` versus key existence.
4. One code-review or micro-practical task to test transfer beyond recognition questions.
